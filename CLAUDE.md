# 알림톡 템플릿 관리 프로그램 — 개발 노트

## 이 프로젝트는 무엇인가
카카오 알림톡 템플릿(코드/상태/메시지유형/본문/버튼/발송조건 등)을
Confluence 표 대신 관리하기 위한 웹 앱. 실제 카카오톡 화면과 유사한
미리보기 기능 포함.

## 구성 요소 (3곳에 나눠져 있음)

| 구성 요소 | 위치 | 역할 |
|---|---|---|
| 앱 화면(클라이언트) | GitHub 저장소 `index.html` | 사용자가 보는 UI, 이 저장소가 소스 오브 트루스 |
| 배포 URL | GitHub Pages | `https://[계정or조직명].github.io/kakao-alimtalk-manager/` |
| 데이터 저장소 | 구글 스프레드시트 (`templates` 시트) | 템플릿 데이터 원본 |
| 이미지 저장소 | 구글 드라이브 폴더 | 업로드된 템플릿 이미지 원본 |
| 백엔드 API | 구글 Apps Script (스프레드시트에 연결됨) | 저장/조회/삭제/이미지 업로드 처리. 코드 백업: `apps-script-backup.gs` |

## 백엔드 접근 권한 / 보안 모델 (2026-08-31 변경됨)
**중요한 배경**: 원래 메인 Apps Script 배포는 "meatbox.co.kr 조직 계정 제한"이었고 몇 달간 잘 동작했으나,
2026-08-31에 원인 불명으로 이 설정에서 브라우저 `fetch()` 요청이 전부 CORS 에러(`net::ERR_FAILED 302`,
`No 'Access-Control-Allow-Origin' header`)로 막히는 현상이 발생함. 실행 로그상 `doGet`/`doPost`는 매번
정상 완료(성공)로 찍혔음에도 브라우저가 응답을 못 읽는 상태였음 — 즉 서버 로직이 아니라 구글이
조직 제한 배포의 응답을 내려줄 때 거치는 리다이렉트 단계에서 CORS 헤더가 빠지는 것으로 추정됨(원인 미확정).
새 배포를 만들어도, 코드를 오늘 변경분 이전으로 되돌려도 동일하게 재현되어 **부득이하게 메인 배포
접근 권한을 "모든 사용자(Anyone)"로 변경**함. 나중에 이 CORS 문제가 우연히 해결되거나 원인이 밝혀지면
다시 조직 제한으로 되돌리는 걸 고려할 것 (그때는 `WRITE_SECRET` 토큰 방식도 같이 걷어낼지 판단 필요).

**현재 보안 모델**: 메인/읽기전용 배포 모두 "모든 사용자" 접근이라 구글 계정 로그인 자체가 보안 경계 역할을
못 함. 대신 `apps-script-backup.gs`의 `WRITE_SECRET` 상수(비밀 토큰)로 쓰기(저장/삭제/이미지 업로드/삭제)를
막는다 — `index.html`의 동일한 `WRITE_SECRET` 값을 요청 본문에 실어 보내야 `assertWriteAllowed()`를 통과함.
**이 저장소는 Public이라 이 토큰도 사실상 노출된 것과 같음** — 완벽한 보안이 아니라 "URL만 알면 아무나 쓰기
가능"한 상태를 막는 최소한의 장치. 두 파일의 `WRITE_SECRET` 값은 항상 동일하게 유지할 것.

## 뷰 전용 모드 / 외부 공유용 URL
쿼리 파라미터로 3가지를 조합해서 쓴다. 화면(`index.html`)은 항상 동일한 파일 — 별도 페이지를 만드는 게 아니라 파라미터로만 분기한다.

- `?view=1`: 완전 읽기 전용 모드 — 조회/검색/필터/미리보기/내보내기만 가능. 새 템플릿 등록·수정·저장·삭제·이미지 업로드/삭제·버튼 추가/삭제는 UI 자체가 숨겨지거나 비활성화됨
  - 이것만으로는 **화면단 제한일 뿐** — 개발자도구로 우회하거나 URL에서 파라미터를 떼고 `WRITE_SECRET` 값을 소스에서 읽어내면 그대로 쓰기 요청을 만들 수 있음
- `?demo=1`: 실제 구글 시트/Apps Script에 **전혀 연결하지 않고** `seedData()`의 샘플 템플릿만 보여줌 (`isRemoteConfigured()`가 항상 false). 완전히 오프라인 데모가 필요할 때 사용
- `?sid=<읽기전용 배포의 스크립트ID>`: **`view=1`이 함께 있을 때만** 동작 — `https://script.google.com/macros/s/{sid}/exec`를 조립해서 백엔드 요청 대상을 이걸로 바꿔치기한다. 전체 URL 대신 ID만 받아서 공유 링크를 짧게 유지함 (앞뒤 고정 부분은 코드에 이미 박혀 있음). 실제(진짜) 템플릿 데이터를 외부 인력에게 보여주되 쓰기는 원천 차단하고 싶을 때 사용 (아래 "외부에 실제 데이터 공유하기" 참고). `view=1` 없이는 무시됨 — 편집 모드에서 임의의 `sid=`를 허용하면 저장 요청이 공격자가 지정한 서버로 전송되는 위험이 있어서 의도적으로 막아둠
  - 이 재조립 방식은 새 배포 URL이 `https://script.google.com/macros/s/{ID}/exec` 형태(도메인 표시 없는 일반 형태)라는 전제로 만들어져 있음. 나중에 배포를 다시 만들어서 URL 형태가 달라지면 코드도 같이 고쳐야 함

### 외부에 실제 데이터를 읽기 전용으로 공유하기 (예: 외주 개발자)
1. **같은 Apps Script 프로젝트에 새 배포를 하나 더 만든다** (배포 관리 → 새 배포 → 웹 앱). 코드는 메인 배포와 동일한 스크립트를 그대로 씀
2. 이 새 배포의 "액세스 권한"도 **"모든 사용자(Anyone)"**로 설정 (메인과 동일 — 별도 도메인 제한을 걸어봤자 위 CORS 문제로 어차피 못 씀)
3. 쓰기(저장/삭제/이미지 업로드/삭제)는 `assertWriteAllowed()`가 `WRITE_SECRET` 토큰을 확인해서 막아줌 — 클라이언트가 `?view=1`로 접속하면 애초에 쓰기 UI 자체가 없어서 토큰을 보낼 일이 없음
4. 새 배포 URL(`https://script.google.com/macros/s/{ID}/exec`)에서 `{ID}` 부분만 복사. **이 ID/URL은 절대 이 저장소에 커밋하지 말 것** (Public 저장소라 그대로 노출됨). 외주 개발자에게는 아래 형태로 링크만 직접 전달:
   `https://[페이지 주소]/index.html?view=1&sid=<복사한 ID>`
5. 템플릿이 새로 등록/수정/삭제되면 같은 스프레드시트를 보는 것이므로 이 링크에도 실시간으로 그대로 반영됨. UI가 바뀌어도(코드 push) 같은 `index.html`이라 동일하게 반영됨

## 데이터 모델
- 템플릿 필드: `id, code, name, status, messageType, content, varExample, buttons[], sendTiming, sendTarget, note, hasImage, imageUrl, updatedAt, createdAt`
- `varExample`: 템플릿 내용(`content`)의 `#{변수}`에 실제 값을 채운 예시를 사용자가 직접 입력하는 필드(선택 입력, 자동 생성 아님). 미리보기 상단의 "변수 적용 전/후" 토글로 확인 — 기본값은 "적용 전"(`content` 그대로 표시)
- 상태(status) 8단계: 선택(기본값) / 검수 전 / 검수 중 / 검수완료 / 사용중 / 휴면 / 중지 / 차단
- 메시지유형(messageType): 기본형(기본값) / 이미지형 — **강조표기형·아이템리스트형은 의도적으로 제외**
- 버튼 타입 11종: 웹링크(기본값) / 앱링크 / 배송조회 / 봇키워드 / 봇전환 / 상담톡전환 / 메시지전달 / 비즈니스폼 / 전화하기 / 지도보기 / 채널 추가
  (타입별로 필요한 입력 필드가 다름 — 코드의 `BUTTON_TYPE_CONFIG` 참고)
- 웹링크/앱링크 버튼에는 `realUrlMobile`/`realUrlPc`(실랜딩 URL - Mobile/PC, 선택 입력) 필드가 있음 — 카카오 심사용 `#{랜딩URL}` 변수와 별개로, 실제로 연결되는 페이지를 Mobile/PC 구분해서 팀 내부 참고용으로 기록하는 필드
- 버튼 타입 선택 시 자동 채움(모두 수정 가능): 웹링크 → Mobile/PC 링크에 `https://#{랜딩URL}` 자동 입력, 전화하기 → 버튼명이 비어 있으면 "고객센터 연결" 자동 입력. 이미 값이 있으면 덮어쓰지 않음
- 템플릿 목록(사이드바·전체 목록 카드)은 **등록일(createdAt) 기준 최근 등록순**으로 정렬됨. 수정일(updatedAt) 변경은 정렬 순서에 영향 없음

## 절대 되돌리면 안 되는 결정들
- **강조유형 필드는 제거됨** — 요청에 따라 삭제. 되살리지 말 것.
- **메시지유형은 기본형/이미지형만 지원**
- **미리보기 UI는 실제 카카오톡 캡처 화면 기준으로 맞춘 것** — 파란 배경, 가짜 상태바/내비바, 프로필 이미지, 발신자명 전부 의도적으로 제거됨
- **전체 목록 카드에는 본문 내용 노출 안 함** (코드/이름/상태/메시지유형만)
- **저장 방식**: 구글 시트에 행 단위로 저장 (`window.storage` 아님 — Claude 환경 밖에서도 동작해야 해서 fetch 기반으로 전환함)
- **이미지**: base64로 시트에 저장하지 않고, 구글 드라이브에 파일로 올린 뒤 URL만 시트에 저장 (용량 문제 때문)
- **updatedAt은 한국 시간(KST)으로 저장** (`Utilities.formatDate(..., 'Asia/Seoul', ...)`, UTC 아님)
- **hasImage가 false일 때 시트에는 빈 칸으로 저장** (FALSE 텍스트로 표기하면 "이미지 등록 실패"로 오해할 수 있어서)

## 배포 관련 주의사항
- Apps Script 코드를 수정하면 **반드시 "배포 관리 → 편집 → 새 버전 → 배포"**까지 해야 실제 반영됨 (저장만 하면 반영 안 됨)
- 배포 액세스 권한: 현재 meatbox.co.kr 조직 계정으로 제한 (사내 전용 도구이므로 의도적 설정)
- GitHub 저장소는 Public — Apps Script URL이 코드에 노출되지만, 실제 데이터 접근은 meatbox.co.kr 구글 로그인이 있어야 가능해서 허용 범위로 판단함

## 다음에 하고 싶은 것 (TODO)
- [ ] 회사 도메인(meatbox.co.kr) 서브도메인 연결 (예: alimtalk.meatbox.co.kr)
- [ ] (필요 시 이후 요청사항을 여기에 계속 적어두기)

## 새 대화에서 이어서 개발하고 싶을 때
아래 내용을 새 대화에 붙여넣으면 바로 이어서 작업 가능합니다.

```
알림톡 템플릿 관리 앱을 이어서 개발하고 싶어.
- GitHub 저장소: https://github.com/gylim92/kakao-alimtalk-manager
- 이 저장소의 PROJECT.md와 index.html, apps-script-backup.gs를 먼저 읽어줘.
- [여기에 하고 싶은 작업 내용 작성]
```
